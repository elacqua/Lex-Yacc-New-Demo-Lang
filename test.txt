START

int a = 5;
a = 7;

if a < 10:{
    print a;
    }
else:{
    print "a is greater than  10";
    }

int array[] = {2,3,4,5,6,7,8,9};
Graph Graph_a(1,2,3);

    #Built in functions#
SHOWONMAP(19,151);
SEARCHLOCATION(array);
GETROADSPEED (graph_a);
GETLOCATION (array) ;
SHOWTARGET (array) ;
ADDROAD (graph_a, array);
SHOWCROSSROADS (graph_a);
SHOWROADS (graph_a);

float counter = 5.5;
while a > 10 {
    print a;
    a = a + 1;
}

int def Swap(x,y){
    a = x;
    b = y;
    x = b;
    y = a;
    return{x,y};
}

int counter2;
for(counter2 = 0; counter2 < 10 ; counter2 = counter2 + 2){
    input x;
}
Swap(5,9);
if x == 5 || x < 8:{
    if a < 10 && a > 4:{
        if counter != 7: {
            print "body can not be empty :) ";
        }
        else:{
            print darling;
            }
    }

}

FINISH